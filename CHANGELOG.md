# 更新日志

## [v1.1.0] - 2026-08-30

### 新增
- 记事本：标题栏实时时钟（秒级刷新）、Ctrl+滚轮/加减号字体缩放（10-32px）、状态栏右下角置顶开关（状态持久化注册表）
- 记事本：行距加大（RichEditBox + 段落行距 1.05）、窗口常驻（关闭 = 最小化到托盘，内容/状态/字体跨开窗保留）
- 设置窗口：卡片分区布局、开机自启置顶开关、快捷键弹窗录制自动聚焦、行内重置按钮、关于卡片
- 记事本与设置窗口：尺寸/位置/字体大小记忆（config.json），位置超屏自动回主屏居中
- 托盘：左键单击打开记事本，菜单项紧凑化
- 应用图标：重绘亮蓝版后改回深灰金配色，ico 小尺寸帧改 BMP 并补 AND 掩码修复拉伸/马赛克
- 开发脚本 scripts/dev.ps1（一键构建重启）

### 变更
- 移除记事本 AI 助手与网络测速功能
- 移除切换亮暗功能（PowerToys 已有），保留"自动亮暗"后一并移除；改名"切换投屏"
- 设置窗口一栏改两栏再回一栏；恢复默认按钮改行内快捷键重置
- 投屏默认热键改为 F14

### 修复
- WinUI 3 窗口显示黑屏/黑白帧：DWM Cloak、延迟显示等方案演进，最终采用 OverlappedPresenter.PreferredMinimum 官方 API 设最小尺寸
- 窗口最小尺寸：弃用子类化（致崩溃），官方 API 解决
- DisplayArea 校验失败不阻塞窗口打开
- 重复开关记事本崩溃、切换投屏后应用退出、设置窗口缩没/打不开
- Logger 生命周期、热键热重载等历史问题（随重构同步修复）

## [v1.0.0] - 2026-08-29

### 变更
- 整体迁移至 WinUI 3（.NET 8 + Windows App SDK 2.4）：托盘、热键、系统集成层保留并重写为 WinUI 3 版本
- 应用模式重构为两个独立窗口：快捷记事（带 AI）与一键切换（亮暗/屏幕配置），托盘菜单精简为入口
- 配置升级为 v3 对象格式，旧 v2 数组格式启动时自动迁移并回写
- 热键绑定从手改 config.json 改为界面录制（保存即热重载，无需重启）
- 发布产物改为自包含（免装 .NET runtime），不再要求 net481 零依赖单 exe

### 新增
- 快捷记事：剪贴板建议条（打开时若剪贴板有文本且与正文不同，点击即可追加）
- AI 助手：把记事本内容发给 OpenAI 兼容端口（自定义 Base URL / API Key / 模型），SSE 流式打字机回复，可折叠面板
- 一键切换窗口：亮暗（热键录制 + 日出日落自动切换配置）与屏幕（热键录制 + 切换模式列表）
- 热键录制控件（HotkeyInputBox）：聚焦后按下组合键即捕获，Esc 取消，一键清除

### 修复
- WinUI 3 发布产物缺失 XBF/PRI 资源导致启动崩溃（0xc000027b）：发布 Target 显式补齐
- 热键回调使用已释放日志导致的进程崩溃：Logger 生命周期改为应用级

### 移除
- 自绘 Win11 托盘菜单（ModernMenu）、自绘弹窗、WinForms 记事本窗口（由 WinUI 3 原生控件替代）
- MiniJson（配置解析改用 System.Text.Json）
- 单 exe / 零外部依赖约束（WindowsAppSDK 引入框架依赖，发布为自包含目录）


## [v0.3.0] - 2026-08-23

### 变更
- 项目更名为 With Windows：程序集/命名空间（`WithWindows`）、解决方案/工程文件（`WithWindows.sln`）、数据目录（`%APPDATA%\WithWindows`）、注册表键（`HKCU\Software\WithWindows`）、单实例互斥体同步调整；GitHub 仓库同步改为 `Lifeni/with-windows`

### 修复
- 快捷记事：大量内容时滚动条不出现（判定只看末尾字符，被编辑控件自动滚动干扰），改为首/尾行双端判定
- 托盘图标：左键点击不再弹出菜单（仅右键弹出）

## [v0.2.0] - 2026-08-15

### 新增
- 快捷记事改为极简纯文本编辑器：原生多行文本框、Maple Mono 等宽字体（含中文）、行/列/字符数显示在标题栏
- 撤回/恢复（Ctrl+Z / Ctrl+Y），历史跨窗口开关保留（持久化到 undo.dat）
- Ctrl+加号/Ctrl+减号/Ctrl+0/Ctrl+滚轮缩放文本（70%～250%）
- F13 显示时复制内容到剪贴板并关闭窗口（重新打开自动恢复内容）
- 自动换行、滚动条按内容显隐、内容实时自动保存

### 移除
- 代码高亮、代码运行、工具栏按钮、状态栏、置顶按钮（窗口始终置顶）
- 语法高亮器与相关测试

## [v0.1.1] - 2026-08-15

### 新增
- 时区判定固定为北京时间（UTC+8），不随机器时区变化
- 托盘菜单增加版本号显示，点击跳转 GitHub 项目页
- GitHub Actions 自动构建并发布 Release（正文取自本文件对应章节）
- CHANGELOG 更新日志

### 变更
- 默认坐标调整（文档不再标注具体城市与坐标）
- README 精简并补齐发布说明

### 修复
- README 热键范围半角波浪号渲染异常（改全角 ～）
- 日出日落测试对机器时区的依赖

### 其他
- 文案规范补充：git 提交信息同样使用中文标点
- AGENTS 增加"改动后必须复查 README 与 CHANGELOG"规矩

## [v0.1.0] - 2026-08-15

### 新增
- 投影切换动作（internal / extend / external / clone / toggle）
- Windows 亮暗切换动作（light / dark / toggle）
- 日出日落自动切换（NOAA 算法，内置默认坐标，可覆盖或使用固定时间）
- 开机自启开关（HKCU Run，用户级）
- Win11 风格自绘托盘菜单（圆角实色卡片、亮暗主题跟随、图标、勾选位、快捷键提示）
- 打开配置、重启应用入口
- 单实例守卫、日志降级
- 配置热键可省略（声明式条目，如 auto_theme）
- 单 exe 发布（net481，零外部依赖）
- GitHub Actions 自动构建并发布 Release

### 修复
- 托盘菜单点击外部不关闭（宿主激活 + 低级鼠标钩子兜底）
- 菜单文字悬停闪烁（实色卡片 + 双缓冲）
- 菜单项文字换行、边距不齐、勾选项文字未对齐
- 重复启动崩溃（日志文件互斥）
- 日出日落测试对机器时区的依赖

### 其他
- AGENTS.md AI Agent 开发指南
- MIT 开源协议
- README 标注 AI 协作完成
- 文案规范：中文标点、中英文间加空格
